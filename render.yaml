services:
  - type: web
    name: doxnaf-online
    runtime: node
    buildCommand: npm install
    startCommand: npm start
    disk:
      name: leaderboard-data
      mountPath: /data
      sizeGB: 1
    envVars:
      - key: NODE_ENV
        value: production
      - key: DATA_DIR
        value: /data
